This is a simple project made using React.js and Node.js.
The main goal of this project is to show how large data (5000+ records) can be displayed smoothly without slowing down the browser.What this project does

The backend automatically creates new data again and again
The frontend shows this data in a list
Only the visible data is shown on the screen
This keeps the app fast even with a lot of records
